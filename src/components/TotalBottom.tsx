/*
    if any items are selected to be ordered, then display the total amount of the order to be placed

    if we are in the order's page, then display confirm order option
*/

import React from 'react'

const TotalBottom = () => {
  return (
    <div>TotalBottom</div>
  )
}

export default TotalBottom